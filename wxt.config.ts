import { defineConfig } from "wxt";

export default defineConfig({
  manifest: {
    name: "Do Some Work Now",
    description: "A browser extension to help you focus and do some work.",
    version: "0.0.1",
  },
});
